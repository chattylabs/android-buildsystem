apply plugin: 'com.chattylabs.versioning'

versioning {

    tagPrefix "version/"

    keywords {
        minor "[feature]"
        patch "[bug]", "[patch]"
    }
}

android {

    defaultConfig {

        versionCode versioning.code()
        versionName versioning.name()
    }
}